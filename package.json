{
  "name": "alive-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
  }
}
